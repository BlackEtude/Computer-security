## Crypto Secure Banking

This is a prototype of banking site created in `django` framework.  


### Requirements 
To run this page, 
packages `django` and `django-tables2` must be installed 
on your machine. 

##### Installation

```pip install django django-tables2```